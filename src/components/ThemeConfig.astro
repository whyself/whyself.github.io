<script is:inline>
  (() => {
    const root = document.documentElement;
    let storedTheme = null;
    try {
      storedTheme = localStorage.getItem("theme");
    } catch {
      storedTheme = null;
    }
    const prefersDark = window.matchMedia && window.matchMedia("(prefers-color-scheme: dark)").matches;
    const resolvedTheme = storedTheme === "dark" || storedTheme === "light" ? storedTheme : (prefersDark ? "dark" : "light");
    root.classList.remove("light", "dark");
    root.classList.add(resolvedTheme);
    root.dataset.theme = resolvedTheme;
    document.querySelector('meta[name="theme-color"]')?.setAttribute("content", resolvedTheme === "dark" ? "#1a1a1a" : "#ffffff");
    document.querySelector('meta[name="msapplication-TileColor"]')?.setAttribute("content", resolvedTheme === "dark" ? "#1a1a1a" : "#ffffff");
  })();
</script>
<style is:global>
  html,
  body {
    transition: background-color 0.28s ease, color 0.28s ease;
  }
  ::view-transition-old(root),
  ::view-transition-new(root) {
    animation: none;
    mix-blend-mode: normal;
  }
  .dark .bg-surface {
    background-color: #1a1a1a !important;
  }
  .dark .bg-surface-dim {
    background-color: #202020 !important;
  }
  .dark .bg-surface-container-lowest {
    background-color: #242424 !important;
  }
  .dark .bg-surface-container-low {
    background-color: #292929 !important;
  }
  .dark .bg-surface-container {
    background-color: #2f2f2f !important;
  }
  .dark .bg-surface-container-high {
    background-color: #363636 !important;
  }
  .dark .bg-surface-container-highest {
    background-color: #3f3f3f !important;
  }
  .dark .text-on-surface {
    color: rgba(255, 255, 255, 0.87) !important;
  }
  .dark .text-on-surface-variant {
    color: rgba(235, 235, 235, 0.68) !important;
  }
  .dark .text-outline {
    color: rgba(235, 235, 235, 0.48) !important;
  }
  .dark .border-surface-container-high {
    border-color: rgba(84, 84, 84, 0.65) !important;
  }
  .dark .border-surface-container-highest {
    border-color: rgba(84, 84, 84, 0.78) !important;
  }
  .dark .border-outline-variant {
    border-color: rgba(84, 84, 84, 0.65) !important;
  }
  .dark .bg-outline-variant {
    background-color: rgba(235, 235, 235, 0.42) !important;
  }
  .dark .text-primary {
    color: rgba(255, 255, 255, 0.9) !important;
  }
  .dark .border-primary {
    border-color: rgba(255, 255, 255, 0.9) !important;
  }
  /* Shiki dual themes: honor light/dark token colors generated by Astro */
  .astro-code,
  .astro-code span {
    color: var(--shiki-light) !important;
    background-color: var(--shiki-light-bg) !important;
  }
  .dark .astro-code,
  .dark .astro-code span {
    color: var(--shiki-dark) !important;
    background-color: var(--shiki-dark-bg) !important;
  }
</style>
